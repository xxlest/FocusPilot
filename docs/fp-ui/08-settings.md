# Settings 页面设计

> **状态**：可开发
> **更新**：2026-05-31
> **原型**：[00-layout-prototype.html](00-layout-prototype.html)
> **参考**：Multica Settings（侧边栏分区 + 右侧表单）

---

## 1. 定位

Settings 是全局配置中心，管理用户偏好和各功能模块的默认行为。

一句话职责：Settings 管**全局默认值和个人偏好**，具体 Agent 配置去 AICrew。

### 与 AICrew 的职责边界

| 维度 | Settings | AICrew |
|------|----------|--------|
| 管理对象 | 全局偏好和默认值 | Agent 成员的详细配置 |
| Agent 相关 | 引用默认 Agent、全局并发上限 | 管理每个 Agent 的角色/能力/MCP |
| 典型问题 | "默认派给哪个 Agent？" | "代码工程师能不能访问 GitHub？" |

**硬规则**：Settings 管全局默认值和偏好，具体 Agent 配置去 AICrew，不重复。

---

## 2. 侧边栏

按两组分区导航，上组「我的账号」管人和机器的偏好，下组「FocusPilot」管各功能模块。

```
┌─ 设置 ────────────────────────┐
│                                │
│ 我的账号                       │
│ 👤 个人资料                    │  <- 默认选中
│ 🎨 偏好设置                    │
│ 🔔 通知                        │
│ ⌨️  快捷键                      │
│ 🖥️ Daemon                     │
│ ⬇️  更新                       │
│ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │
│ FocusPilot                     │
│ ⚙️ 通用                        │
│ 💻 Studio                     │
│ 🤖 AICrew                     │
└────────────────────────────────┘
```

点击分区 → 工作区切换到对应配置表单。

---

## 3. 工作区

### 3.1 个人资料

```
┌─────────────────────────────────────────────┐
│ 个人资料                                     │
│                                              │
│ ┌────────────────────────────────────────┐  │
│ │  (🧑‍💻)  点击上传头像                      │  │
│ │                                        │  │
│ │  姓名                                  │  │
│ │  [Bruce                          ]     │  │
│ │                                        │  │
│ │  关于你                                │  │
│ │  [例如：后端工程师（Swift + macOS）。   │  │
│ │   喜欢简洁的 PR，改动同时附上测试。]   │  │
│ │                               0/2000   │  │
│ │  会随任务一起发送给 Agent——角色、      │  │
│ │  技术栈、偏好，任何你会在第一天告诉    │  │
│ │  新同事的内容。                         │  │
│ │                       [📋 更新资料]    │  │
│ └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

| 字段 | 控件 | 说明 |
|------|------|------|
| 头像 | 圆形占位 + emoji 选择 | V1 只支持 emoji，不做文件上传 |
| 姓名 | input | 显示在 AICrew 运行记录中 |
| 关于你 | textarea（2000 字限制） | 随任务发送给 Agent 的上下文 |
| 更新资料 | 按钮 | 提交保存 |

### 3.2 偏好设置

```
┌─────────────────────────────────────────────┐
│ 偏好设置                                     │
│                                              │
│ 主题                                         │
│ 浅色                                         │
│ ┌────┐ ┌────┐ ┌────┐ ┌────┐                 │
│ │侧栏│ │侧栏│ │侧栏│ │侧栏│                 │
│ │默认白│ │暖象牙│ │薄荷绿│ │淡天蓝│                 │
│ └────┘ └────┘ └────┘ └────┘                 │
│ 深色                                         │
│ ┌────┐ ┌────┐ ┌────┐ ┌────┐                 │
│ │侧栏│ │侧栏│ │侧栏│ │侧栏│                 │
│ │经典深│ │深海蓝│ │ 墨绿 │ │ 纯黑 │                 │
│ └[选中]┘ └────┘ └────┘ └────┘                 │
│                                              │
│ 悬浮球                                       │
│ ┌────────────────────────────────────────┐  │
│ │ 显示悬浮球                      [●──] │  │
│ ├────────────────────────────────────────┤  │
│ │ 大小          ▬▬▬▬▬▬●▬▬▬▬     37px   │  │
│ ├────────────────────────────────────────┤  │
│ │ 透明度        ▬▬▬▬▬▬▬▬▬▬●     100%   │  │
│ └────────────────────────────────────────┘  │
│                                              │
│ 快捷面板                                     │
│ ┌────────────────────────────────────────┐  │
│ │ 透明度        ▬▬▬▬▬▬▬▬▬▬●     100%   │  │
│ ├────────────────────────────────────────┤  │
│ │ 弹出动画      ▬▬▬▬▬●▬▬▬▬     250ms   │  │
│ ├────────────────────────────────────────┤  │
│ │ hover 离开后自动收起面板        [●──] │  │
│ └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

| 分组 | 字段 | 控件 | 说明 |
|------|------|------|------|
| 主题 | 主题 | 8 张预览卡片（浅色 4 + 深色 4） | 沿用 FocusPilot 8 色主题系统：浅色（默认白/暖象牙/薄荷绿/淡天蓝）、深色（经典深/深海蓝/墨绿/纯黑）。每张卡片展示侧栏、内容底色、文字条和 accent 点，选中即时切换全站主题。默认选中经典深；默认白与经典深标记为推荐 |
| 悬浮球 | 显示悬浮球 | toggle | 控制悬浮球可见性 |
| | 大小 | slider（24–56px） | 球体尺寸，默认 37px |
| | 透明度 | slider（20–100%） | 球体透明度，默认 100% |
| 快捷面板 | 透明度 | slider（20–100%） | 面板透明度，默认 100% |
| | 弹出动画 | slider（0–500ms） | 弹出动画时长，默认 250ms。0ms = 关闭动画 |
| | hover 离开后自动收起 | toggle | 默认开启 |

> **V1 变更**：原"面板默认尺寸"（小/中/大）已裁剪，面板尺寸由布局自适应决定，不再暴露选项。

### 3.3 通知

```
┌─────────────────────────────────────────────┐
│ 通知                                         │
│                                              │
│ 收件箱通知                                   │
│ 控制哪些事件会产生收件箱通知。               │
│ ┌────────────────────────────────────────┐  │
│ │ 任务分配                        [●──] │  │
│ │ 有新 Task 分配给你时                   │  │
│ ├────────────────────────────────────────┤  │
│ │ 状态变更                        [●──] │  │
│ │ 关注的 Task 状态变化时                 │  │
│ ├────────────────────────────────────────┤  │
│ │ 智能体活动                      [●──] │  │
│ │ Agent task 完成或失败时                │  │
│ ├────────────────────────────────────────┤  │
│ │ 专注计时                        [●──] │  │
│ │ 番茄钟阶段完成提醒                     │  │
│ └────────────────────────────────────────┘  │
│                                              │
│ 系统通知                                     │
│ 控制 FocusPilot 在后台时是否弹出通知横幅。   │
│ ┌────────────────────────────────────────┐  │
│ │ 显示系统通知                    [●──] │  │
│ │ App 未获焦点时弹出 macOS 通知横幅      │  │
│ ├────────────────────────────────────────┤  │
│ │ 通知声音                        [──●] │  │
│ │ 搭配系统通知播放提示音                 │  │
│ └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

### 3.4 快捷键

```
┌─────────────────────────────────────────────┐
│ 快捷键                                       │
│                                              │
│ 全局快捷键                                   │
│ 点击快捷键区域，按下新组合键即可录入。       │
│ ┌────────────────────────────────────────┐  │
│ │ 显示/隐藏面板               [⌘⇧B]    │  │
│ ├────────────────────────────────────────┤  │
│ │ 快速录入                    [⌘⇧N]    │  │
│ ├────────────────────────────────────────┤  │
│ │ 切换 Focus 模式             [⌘⇧F]    │  │
│ └────────────────────────────────────────┘  │
│                                              │
│ 录入时如果与已有快捷键冲突，会显示红色提示。 │
└──────────────────────────────────────────────┘
```

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 显示/隐藏面板 | ⌘⇧B | 全局呼出/隐藏 Quick Panel |
| 快速收集 | ⌘⇧N | 全局呼出快速录入弹窗（录入到 Projects 页 Inbox Tab） |
| 切换 Focus 模式 | ⌘⇧F | 快速进入/退出专注 |

录入交互：点击快捷键 pill → 进入录入态（"按下快捷键…"）→ 按键组合回显 → 冲突检测（红色 pill "与 XX 冲突"）。

### 3.5 Daemon

```
┌─────────────────────────────────────────────┐
│ Daemon                                       │
│ 配置本地 Agent daemon 与桌面应用的协作行为。 │
│                                              │
│ 随 App 启动                           [●──] │
│ 打开 FocusPilot 时自动启动 daemon。          │
│                                              │
│ 退出时停止                            [──●] │
│ 关闭 App 时停止 daemon。关闭则后台常驻。     │
│                                              │
│ CLI 状态                                     │
│ coder-bridge CLI 已安装，路径 ~/.coder-bridge│
│                                              │
│ 诊断信息                                     │
│ ┌─────────────┬──────────────────────────┐  │
│ │ State       │ ● Running                │  │
│ │ Uptime      │ 6h 3m                    │  │
│ │ PID         │ 85058                    │  │
│ │ Daemon ID   │ 019e6da2                 │  │
│ │ 设备名      │ MacBook-Pro-10.local     │  │
│ │ 执行器      │ 5                        │  │
│ └─────────────┴──────────────────────────┘  │
└──────────────────────────────────────────────┘
```

### 3.6 更新

```
┌─────────────────────────────────────────────┐
│ 更新                                         │
│ 桌面端每小时自动检查新版本，并在后台下载。   │
│ 更新就绪后会提示你重启。                     │
│                                              │
│ 当前版本                                     │
│ v0.0.1                                       │
│                                              │
│ 检查更新                      [立即检查]     │
│ 立即检查更新，而不是等待下一次自动轮询。     │
└──────────────────────────────────────────────┘
```

### 3.7 通用（FocusPilot）

| 字段 | 控件 | 说明 |
|------|------|------|
| 开机自启 | toggle | 登录时自动启动 FocusPilot |
| 数据存储目录 | 路径显示 + 修改按钮 | 项目数据根目录 |

### 3.8 Studio（含原 Focus 配置）

| 分组 | 字段 | 控件 | 说明 |
|------|------|------|------|
| 工作项 | 默认执行方式 | radio：普通 / 对话 / 自动 | 新建 Task 默认执行方式 |
| | 默认 Agent | select（引用 AICrew 成员） | 未指定时派给谁 |
| | 默认启用评估 | toggle | 新建 Task 默认是否启用评估 |
| | 并发上限 | select：1 / 2 / 3 | 全局最大并行 Task 数 |
| 对话 | 默认 Agent | select（引用 AICrew 成员） | Studio 对话默认执行者 |
| | 代码字体大小 | select：12 / 13 / 14 / 15 | 代码块渲染字号 |
| Workspace | 默认 Workspace 根目录 | 路径 + 修改按钮 | 临时 Workspace 创建位置 |
| | 自动清理天数 | select：7 / 14 / 30 / 永不 | done/cancelled 后保留天数 |

### 3.10 AICrew

| 字段 | 控件 | 说明 |
|------|------|------|
| 默认新建 Runtime | select | 新建智能体时预选的 Runtime |
| Session 超时 | select：5min / 15min / 30min / 1h | Agent 空闲多久自动释放执行槽 |
| 前往 AICrew → | accent 色链接 | 跳转到 AICrew 页面管理成员 |

---

## 4. 核心交互

| 用户任务 | 操作路径 | 规则 |
|----------|----------|------|
| 修改个人资料 | 个人资料 → 编辑 → 更新资料 | 点击按钮提交保存 |
| 切换主题 | 偏好设置 → 点击主题卡片 | 即时预览，无需手动保存 |
| 调节悬浮球 | 偏好设置 → 悬浮球分组 | slider 拖动即时生效 |
| 调节面板 | 偏好设置 → 快捷面板分组 | slider 拖动即时生效 |
| 配置通知 | 通知 → toggle 开关 | 即时生效 |
| 录入快捷键 | 快捷键 → 点击当前值 → 按下新组合键 | 冲突时红色提示，需先解决冲突 |
| 查看 Daemon 状态 | Daemon → 诊断信息 | 只读展示 |
| 检查更新 | 更新 → 立即检查 | 触发检查，有更新时提示重启 |
| 修改默认 Agent | Focus / Studio → 默认 Agent select | 即时生效，引用 AICrew 成员 |
| 跳转 AICrew | AICrew → 前往 AICrew → | 切换到 AICrew 页面 |

---

## 5. 状态与规则

### 保存行为

- 偏好设置（主题/悬浮球/快捷面板）、通知、快捷键、Studio/AICrew 配置：**即时生效**，无需手动保存
- 个人资料：**按钮提交**保存（因为 textarea 内容较长，避免频繁写入）
- 主题切换：**即时预览**

### 快捷键冲突检测

- 录入新组合键时，检查是否与已有快捷键冲突
- 冲突时该行显示红色 pill："与「显示/隐藏面板」冲突"
- 冲突状态下不保存，用户需修改为不冲突的组合键

### V1 不做

| 项目 | 原因 |
|------|------|
| API Token | 个人版无云端 API |
| GitHub 集成 | V1 纯本地 |
| 集成/实验室 | 无第三方集成和实验功能 |
| 成员管理 | 个人版无多用户 |
| 代码仓库 | 项目管理在 Projects |
| 项目模式（Agile/Flow/Lite/Free） | 无页面承载此功能 |

---

## 6. 数据对象

| 对象 | 说明 | 详细定义 |
|------|------|---------|
| Preferences | 全局偏好配置，持久化到 UserDefaults | [Architecture.md §ConfigStore](../Architecture.md) |
| UserProfile | 姓名、头像、关于你，发送给 Agent | V1 新增 |

---

## 7. 术语

| 术语 | 含义 |
|------|------|
| Settings | 页面名称（活动栏 ⚙️） |
| 我的账号 | 侧边栏上组：用户和机器的偏好 |
| FocusPilot（工作区） | 侧边栏下组：各功能模块默认行为 |
| Daemon | 本地 Agent 执行进程 |
| 快捷键录入 | 点击进入录入态 → 按下组合键 → 回显 |

---

*待定项：无。*
